const fs = require('fs'); const pf = 'Festivo_Frontend/src/app/Guest/registerinstitution/registerinstitution.ts'; let ts = fs.readFileSync(pf, 'utf8'); ts = ts.replace('this.router.navigate([\'/guestmaster/guesthome\']);', 'this.router.navigate([\'/guestmaster/guesthome\']).then(() => window.scrollTo(0, 0));'); fs.writeFileSync(pf, ts); console.log('Added auto scroll to top when redirecting home');
