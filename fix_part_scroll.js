const fs = require('fs'); const pf = 'Festivo_Frontend/src/app/Guest/registerparticipant/registerparticipant.ts'; let ts = fs.readFileSync(pf, 'utf8'); ts = ts.replace('this.submitting = true;', 'this.submitting = true;\n      window.scrollTo({ top: 0, behavior: \'smooth\' });'); ts = ts.replace('this.router.navigate([\'/guestmaster/guesthome\']);', 'this.router.navigate([\'/guestmaster/guesthome\']).then(() => window.scrollTo(0, 0));'); fs.writeFileSync(pf, ts); console.log('Added participant scroll to top');
