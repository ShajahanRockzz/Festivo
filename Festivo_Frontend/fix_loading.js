const fs = require('fs'); const file = 'src/app/Guest/festdetailsguest/festdetailsguest.ts'; let content = fs.readFileSync(file, 'utf8'); content = content.replace(/complete: \(\) => \{\s*this\.isLoading = false;\s*\}/, ''); content = content.replace(/this\.festDetails = res\.data;\s*\}/, 'this.festDetails = res.data;\n        }\n        this.isLoading = false;'); content = content.replace(/console\.error\('Error fetching fest details', err\);/, 'console.error(''Error fetching fest details'', err);\n        this.isLoading = false;'); fs.writeFileSync(file, content);
