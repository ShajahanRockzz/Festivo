const fs = require('fs'); const paths = ['Festivo_Frontend/src/app/Guest/registerinstitution/registerinstitution.ts', 'Festivo_Frontend/src/app/Guest/registerparticipant/registerparticipant.ts']; paths.forEach(p => { let src = fs.readFileSync(p, 'utf8'); src = src.replace(/\/guestmaster\/guesthome/g, '/guestmaster/login'); fs.writeFileSync(p, src); }); console.log('Replaced route with login');
